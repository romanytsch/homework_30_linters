test:
  steps:
  - uses: actions/checkout@v4
  - uses: actions/setup-python@v5
    with: {python-version: '3.12'}
  - run: pip install fastapi sqlalchemy pytest "pytest>=7.0"
  - run: pytest tests/ -v --import-mode=prepend
